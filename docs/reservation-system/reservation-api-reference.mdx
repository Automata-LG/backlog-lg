---
title: '예약 시스템 API 레퍼런스'
description: '예약 시스템 REST API 및 tRPC 엔드포인트 상세 문서'
icon: 'code'
---

## 개요

LG 멤버십 예약 시스템은 다음 네 가지 핵심 리소스로 구성됩니다:

| 리소스 | 설명 | 예시 |
| --- | --- | --- |
| **예약 호스트** | 예약 장소의 최상위 개념 | LG 아트센터, LG 사이언스홀 |
| **예약 스페이스** | 호스트 내 특정 공간 | 메인홀, 소극장, 전시관 |
| **예약 컨텐츠** | 특정 스페이스에서 진행되는 구체적인 이벤트/프로그램 | 뮤지컬 '마틸다', 클래식 연주회 |
| **예약 타임슬롯** | 컨텐츠의 특정 시간대 | 2025년 6월 1일 14:00-16:00 |
| **예약** | 사용자가 특정 타임슬롯에 신청한 예약 정보 | 멤버십 번호 123456이 신청한 2명 예약 |

각 리소스는 관리자용(Admin)과 사용자용(App) 두 가지 인터페이스로 API가 제공됩니다.

## API 공통 정보

### 기술 스택

- **백엔드**: tRPC + trpc-openapi, PostgreSQL (Prisma ORM)
- **API 인터페이스**:
  - tRPC: 타입 안전성이 보장되는 기본 API 인터페이스
  - REST: trpc-openapi를 통해 기존 tRPC 라우터를 REST API로도 노출
- **인증**: JWT 기반

### 인증 및 권한

- **관리자 API**: 모든 API 호출 시 인증 필요(JWT)
- **앱 API**: 대부분 인증 불필요(public), 일부 회원 전용 기능만 인증 필요
- **권한 레벨**:
  - publicProcedure: 인증 없이 접근 가능
  - protectedProcedure: 인증된 사용자만 접근 가능
  - adminProtectedProcedure: 관리자/스태프만 접근 가능
  - memberProtectedProcedure: 일반 회원만 접근 가능

### 응답 상태 코드

- **성공**: 200 OK (데이터 반환 또는 반환 없음)
- **에러**: 400, 401, 403, 404, 409, 500 등 상황에 따라 다름

### 페이지네이션 응답 포맷

목록 조회 API는 항상 아래 포맷으로 응답합니다:

```json
{
  "list": [
    // 해당 리소스의 객체 배열
  ],
  "page": 1,          // [필수] 현재 페이지
  "pageSize": 20,     // [필수] 페이지당 항목 수
  "pageCount": 5,     // [필수] 총 페이지 수
  "totalCount": 100   // [필수] 총 항목 수
}
```

### 날짜 및 시간 관련 필드

- **호스트**:
  - `operatingStartAt/operatingEndAt`: 공간 자체의 운영 기간
  - `displayStartAt/displayEndAt`: UI에 표시되는 시작 및 종료 날짜

- **컨텐츠**:
  - `operatingStartAt/operatingEndAt`: 실제 운영 기간 (자동 계산)
  - `openStartAt/openEndAt`: 예약 가능 기간 (자동 계산)
  - `displayStartAt/displayEndAt`: UI에 표시되는 시작 및 종료 날짜

### 진행 상태 필터링

진행 상태 필터(ProgressStatusFilter)를 사용하여 특정 상태의 항목만 조회할 수 있습니다:

- `UPCOMING`: 아직 시작하지 않은 항목
- `IN_PROGRESS`: 현재 진행 중인 항목
- `ENDED`: 이미 종료된 항목

## 주요 데이터 타입 및 스키마

### 예약 상태 (ReservationStatus)

```typescript
enum ReservationStatus {
  PENDING = "PENDING",     // 예약 대기
  CONFIRMED = "CONFIRMED", // 예약 확정
  USED = "USED",          // 이용 완료
  CANCELLED = "CANCELLED", // 예약 취소
  NOSHOW = "NOSHOW"       // 노쇼
}
```

### 예약 상태 변경 주체 (ReservationStatusChanger)

```typescript
export const ReservationStatusChanger = {
  USER: 'USER',     // 사용자에 의한 변경
  ADMIN: 'ADMIN',   // 관리자에 의한 변경
  SYSTEM: 'SYSTEM', // 시스템(자동화)에 의한 변경
} as const;
```

### 예약 시스템 작업 유형 (ReservationSystemAction)

시스템에 의한 상태 변경 시 세부 작업 유형을 나타냅니다:

```typescript
export const ReservationSystemAction = {
  LOTTERY_WIN: 'LOTTERY_WIN',   // 추첨 당첨
  LOTTERY_FAIL: 'LOTTERY_FAIL', // 추첨 낙첨
  EXPIRE: 'EXPIRE',            // 자동 만료
  NOSHOW: 'NOSHOW',            // 자동 노쇼
  QR_VERIFY: 'QR_VERIFY',      // QR 확인
} as const;
```

### 예약 상태 변경 이유 코드 (ReservationStatusReasonCode)

```typescript
export const ReservationStatusReasonCode = {
  // 초기 예약 생성 (사용자/관리자 구분)
  USER_CREATE_PENDING: 'USER_CREATE_PENDING',
  ADMIN_CREATE_PENDING: 'ADMIN_CREATE_PENDING',
  USER_CREATE_CONFIRMED: 'USER_CREATE_CONFIRMED',
  ADMIN_CREATE_CONFIRMED: 'ADMIN_CREATE_CONFIRMED',

  // 상태 변경 - PENDING→CONFIRMED
  ADMIN_CONFIRM: 'ADMIN_CONFIRM',
  SYSTEM_LOTTERY_WIN: 'SYSTEM_LOTTERY_WIN',

  // 상태 변경 - PENDING→CANCELLED
  USER_CANCEL: 'USER_CANCEL',
  ADMIN_CANCEL: 'ADMIN_CANCEL',
  SYSTEM_LOTTERY_FAIL: 'SYSTEM_LOTTERY_FAIL',
  SYSTEM_EXPIRE: 'SYSTEM_EXPIRE',

  // 예약 수정 (상태 유지)
  USER_MODIFY_PENDING: 'USER_MODIFY_PENDING',
  ADMIN_MODIFY_PENDING: 'ADMIN_MODIFY_PENDING',
  USER_MODIFY_CONFIRMED: 'USER_MODIFY_CONFIRMED',
  ADMIN_MODIFY_CONFIRMED: 'ADMIN_MODIFY_CONFIRMED',

  // 상태 변경을 수반하는 예약 수정
  USER_MODIFY_TO_PENDING: 'USER_MODIFY_TO_PENDING',
  ADMIN_MODIFY_TO_PENDING: 'ADMIN_MODIFY_TO_PENDING',

  // CONFIRMED→USED/NOSHOW
  ADMIN_MARK_USED: 'ADMIN_MARK_USED',
  SYSTEM_QR_VERIFY: 'SYSTEM_QR_VERIFY',
  SYSTEM_NOSHOW: 'SYSTEM_NOSHOW',

  // 이용 완료/노쇼 후 취소
  ADMIN_USED_CANCEL: 'ADMIN_USED_CANCEL',
  ADMIN_NOSHOW_CANCEL: 'ADMIN_NOSHOW_CANCEL',
} as const;
```

### 예약 확정 유형 (ConfirmType)

```typescript
export const ConfirmType = {
  IMMEDIATE: 'IMMEDIATE',         // 즉시 확정
  ADMIN_APPROVAL: 'ADMIN_APPROVAL', // 관리자 승인
  LOTTERY: 'LOTTERY',            // 추첨
} as const;
```

#### 즉시 확정 (IMMEDIATE) 설정 예시

```json
{
  "confirmType": "IMMEDIATE"
}
```

#### 관리자 승인 (ADMIN_APPROVAL) 설정 예시

```json
{
  "confirmType": "ADMIN_APPROVAL"
}
```

#### 추첨 (LOTTERY) 설정 예시

```json
{
  "confirmType": "LOTTERY",
  "lotteryDate": "2025-05-15",    // [필수] 추첨일(YYYY-MM-DD)
  "lotteryCapacity": 30           // [필수] 당첨 가능 인원
}
```

### 예약 오픈 타입 (OpenType)

```typescript
export const OpenType = {
  BATCH: 'BATCH',       // 일괄 오픈
  SEQUENTIAL: 'SEQUENTIAL', // 순차 오픈
} as const;
```

#### 일괄 오픈 (BATCH) 설정 예시

```json
{
  "openType": "BATCH",
  "openStartAt": "2025-05-01T12:00:00Z",  // [필수] 예약 오픈 시작
  "openEndAt": "2025-05-31T23:59:59Z",    // [필수] 예약 오픈 종료
  "reservationByHours": 24,               // [옵션] 예약 마감 시간
  "modificationByHours": 48               // [옵션] 수정 마감 시간
}
```

#### 순차 오픈 (SEQUENTIAL) 설정 예시

```json
{
  "openType": "SEQUENTIAL",
  "openStartByDays": 14,      // [필수] 타임슬롯 N일 전 오픈
  "openEndByDays": 2,         // [필수] 타임슬롯 N일 전 마감
  "modificationByHours": 48   // [옵션] 수정 마감 시간
}
```

### 예약 통계 스키마

#### 타임슬롯별 예약 통계

```typescript
const reservationTimeSlotStatsSchema = {
  timeSlotId: String,
  startAt: Date,
  endAt: Date,

  // 수용/잔여 인원
  totalCapacity: Number,
  availableCapacity: Number,

  // 예약 현황
  pendingCount: Number,
  confirmedCount: Number,
  cancelledCount: Number,
  usedCount: Number,
  noshowCount: Number,

  // 인원 통계
  totalPendingSize: Number,
  totalConfirmedSize: Number,
  totalCancelledSize: Number,
  totalUsedSize: Number,
  totalNoshowSize: Number,

  // 복합 계산 수치
  actualReservedSize: Number,
  actualConfirmedSize: Number,
  actualCancelledSize: Number,

  // 비율 통계 (%)
  reservationRate: Number,
  confirmationRate: Number,
  utilizationRate: Number,
  cancellationRate: Number,
  noshowRate: Number,
}
```

## API 엔드포인트

### 1. 예약 호스트 (ReservationHost)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| POST | `/external/admin/reservation/hosts` | 호스트 목록 조회 | 필요 |
| POST | `/external/admin/reservation/host/create` | 호스트 생성 | 필요 |
| POST | `/external/admin/reservation/host/update` | 호스트 수정 | 필요 |
| POST | `/external/admin/reservation/host/delete` | 호스트 삭제 | 필요 |

##### 호스트 목록 조회

**요청**:
```json
{
  "kind": "ART_CENTER",                           // [옵션] 호스트 종류
  "operatingStatusIn": ["UPCOMING", "IN_PROGRESS"], // [옵션] 운영 상태
  "displayStatusIn": ["UPCOMING", "IN_PROGRESS"],   // [옵션] 표시 상태
  "isActive": true,                               // [옵션] 활성화 여부
  "orderBy": "createdAt_desc",                    // [옵션] 정렬 기준
  "page": 1,                                      // [옵션] 페이지 번호
  "pageSize": 20                                  // [옵션] 페이지 크기
}
```

**응답**: 페이지네이션된 호스트 목록

##### 호스트 생성

**요청**:
```json
{
  "kind": "ART_CENTER",              // [필수] 호스트 종류
  "name": "LG 아트센터",               // [필수] 호스트 이름
  "address": {                       // [필수] 주소 정보
    "zipCode": "06159",
    "mainAddress": "서울시 강남구",
    "detailAddress": "LG아트센터"
  },
  "operatingHours": [...],           // [필수] 운영 시간
  "description": "설명",              // [필수] 설명
  "images": ["url1", "url2"],        // [필수] 이미지 URL
  "phoneNumber": "02-2005-0114",     // [옵션] 전화번호
  "operatingStartAt": "2025-01-01",  // [옵션] 운영 시작일
  "operatingEndAt": "2025-12-31",    // [옵션] 운영 종료일
  "holidays": {...},                 // [옵션] 휴무일
  "instructions": "이용 안내",        // [옵션] 이용 안내
  "displayStartAt": "2025-01-01",    // [옵션] 표시 시작일
  "displayEndAt": "2025-12-31"       // [옵션] 표시 종료일
}
```

#### App API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/app/reservation/hosts` | 호스트 목록 조회 | 불필요 |

### 2. 예약 스페이스 (ReservationSpace)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/admin/reservation/spaces` | 스페이스 목록 조회 | 필요 |
| POST | `/external/admin/reservation/space/create` | 스페이스 생성 | 필요 |
| POST | `/external/admin/reservation/space/update` | 스페이스 수정 | 필요 |
| POST | `/external/admin/reservation/space/delete` | 스페이스 삭제 | 필요 |

#### App API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/app/reservation/spaces` | 스페이스 목록 조회 | 불필요 |

### 3. 예약 컨텐츠 (ReservationContent)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| POST | `/external/admin/reservation/contents` | 컨텐츠 목록 조회 | 필요 |
| GET | `/external/admin/reservation/content/get` | 컨텐츠 상세 조회 | 필요 |
| POST | `/external/admin/reservation/content/create` | 컨텐츠 생성 | 필요 |
| POST | `/external/admin/reservation/content/update` | 컨텐츠 수정 | 필요 |
| POST | `/external/admin/reservation/content/delete` | 컨텐츠 삭제 | 필요 |

##### 컨텐츠 생성

**요청**:
```json
{
  "name": "뮤지컬 '마틸다'",           // [필수] 컨텐츠 이름
  "description": "토니상 수상작",      // [필수] 설명
  "images": ["url1", "url2"],       // [필수] 이미지 URL
  "confirmConfig": {                // [필수] 예약 확정 설정
    "confirmType": "LOTTERY",
    "lotteryDate": "2025-05-15",
    "lotteryCapacity": 30
  },
  "hostId": "host-uuid",            // [필수] 호스트 ID
  "spaceId": "space-uuid",          // [옵션] 스페이스 ID
  "instructions": "관람 안내",        // [옵션] 이용 안내
  "contactInfo": {...},             // [옵션] 연락처 정보
  "restrictionConfig": {            // [옵션] 제한 설정
    "minMembershipGrade": "AS03"
  },
  "displayStartAt": "2025-04-01",   // [옵션] 표시 시작일
  "displayEndAt": "2025-05-30"      // [옵션] 표시 종료일
}
```

#### App API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/app/reservation/contents` | 컨텐츠 목록 조회 | 불필요 |
| GET | `/external/app/reservation/content/get` | 컨텐츠 상세 조회 | 불필요 |

### 4. 예약 타임슬롯 (ReservationTimeSlot)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/admin/reservation/time-slots` | 타임슬롯 목록 조회 | 필요 |
| POST | `/external/admin/reservation/time-slot/create` | 타임슬롯 생성 | 필요 |
| POST | `/external/admin/reservation/time-slot/batch-create` | 타임슬롯 일괄 생성 | 필요 |
| POST | `/external/admin/reservation/time-slot/update` | 타임슬롯 수정 | 필요 |
| POST | `/external/admin/reservation/time-slot/delete` | 타임슬롯 삭제 | 필요 |

##### 타임슬롯 일괄 생성

**요청**:
```json
{
  "contentId": "content-uuid",              // [필수] 컨텐츠 ID
  "operatingStartDate": "2025-06-01",       // [필수] 운영 시작일
  "operatingEndDate": "2025-06-30",         // [필수] 운영 종료일
  "minGroupSize": 1,                        // [필수] 최소 그룹 인원
  "maxGroupSize": 4,                        // [필수] 최대 그룹 인원
  "openConfig": {                           // [필수] 오픈 설정
    "openType": "BATCH",
    "openStartAt": "2025-05-01T12:00:00Z",
    "openEndAt": "2025-05-31T23:59:59Z"
  },
  "timeSlotTemplates": [                    // [필수] 타임슬롯 템플릿
    {
      "operatingHours": {
        "days": ["MON", "WED", "FRI"],
        "hours": [{"openTime": "14:00", "closeTime": "16:30"}]
      },
      "totalCapacity": 1100
    }
  ],
  "timeUnit": 60,                           // [옵션] 시간 단위(분)
  "holidays": {...}                         // [옵션] 휴무일
}
```

**응답**:
```json
{
  "count": 50  // 생성된 타임슬롯 개수
}
```

#### App API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/app/reservation/time-slots` | 타임슬롯 목록 조회 | 불필요 |

### 5. 예약 (Reservation)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| POST | `/external/admin/reservations` | 예약 목록 조회 | 필요 |
| GET | `/external/admin/reservation/get` | 예약 상세 조회 | 필요 |
| POST | `/external/admin/reservation/create` | 예약 생성 | 필요 |
| POST | `/external/admin/reservation/update` | 예약 수정 | 필요 |
| POST | `/external/admin/reservation/status/bulk-change` | 예약 상태 대량 변경 | 필요 |

##### 예약 목록 조회

**요청**:
```json
{
  "timeSlotId": "timeslot-uuid",          // [옵션] 타임슬롯 ID
  "contentId": "content-uuid",            // [옵션] 컨텐츠 ID
  "hostId": "host-uuid",                  // [옵션] 호스트 ID
  "memberId": "member-uuid",              // [옵션] 회원 ID
  "statusIn": ["PENDING", "CONFIRMED"],   // [옵션] 상태 필터
  "reasonCodeIn": ["USER_CREATE_PENDING"], // [옵션] 이유 코드 필터
  "startsAfter": "2025-06-01",           // [옵션] 시작일 이후
  "startsBefore": "2025-06-30",          // [옵션] 시작일 이전
  "memName": "홍길동",                    // [옵션] 회원 이름
  "memNo": "1234567890",                 // [옵션] 회원 번호
  "phoneNumber": "+821012345678",        // [옵션] 전화번호
  "sn": "LGR",                           // [옵션] 예약 번호
  "includesStats": false,                // [옵션] 통계 포함
  "shouldMask": true,                    // [옵션] 마스킹 여부
  "orderBy": "createdAt_desc",           // [옵션] 정렬
  "page": 1,                             // [옵션] 페이지
  "pageSize": 20                         // [옵션] 페이지 크기
}
```

**응답**: 페이지네이션된 예약 목록 (통계 정보 포함 가능)

##### 예약 상태 대량 변경

**요청**:
```json
{
  "ids": ["reservation-id-1", "reservation-id-2"],  // [필수] 예약 ID 배열
  "status": "CONFIRMED",                           // [필수] 변경할 상태
  "note": "관리자 일괄 승인"                         // [옵션] 메모
}
```

**응답**:
```json
{
  "successCount": 1,                              // 성공 건수
  "failedIds": ["reservation-id-2"]               // 실패한 ID
}
```

#### App API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| POST | `/external/app/reservation/me/reservations` | 내 예약 목록 조회 | 필요 |
| GET | `/external/app/reservation/me/reservation/get` | 내 예약 상세 조회 | 필요 |
| POST | `/external/app/reservation/create` | 예약 생성 | 필요 |
| POST | `/external/app/reservation/update` | 예약 수정 | 필요 |
| POST | `/external/app/reservation/cancel` | 예약 취소 | 필요 |
| POST | `/external/app/reservation/checkin` | 예약 체크인 | 필요 |

##### 예약 생성

**요청**:
```json
{
  "timeSlotId": "timeslot-uuid",              // [필수] 타임슬롯 ID
  "groupSize": 2,                             // [필수] 그룹 인원
  "phoneNumber": "+821012345678",             // [필수] 전화번호
  "termsAgreements": [                        // [필수] 약관 동의
    {
      "termsId": "terms-uuid",
      "isAgreed": true
    }
  ]
}
```

**응답**:
```json
{
  "id": "reservation-uuid",          // 생성된 예약 ID
  "sn": "LGR-123abc456def789g"       // 생성된 예약 번호
}
```

##### 예약 체크인

**요청**:
```json
{
  "id": "reservation-uuid",     // [필수] 예약 ID
  "note": "현장 도착 확인"       // [옵션] 메모
}
```

### 6. 회원 정보 (Member)

#### Admin API

| 메소드 | 엔드포인트 | 설명 | 인증 |
| --- | --- | --- | --- |
| GET | `/external/admin/member/get` | 회원 상세 조회 | 필요 |

##### 회원 상세 조회

**파라미터**:
- `memNo`: 회원번호 [필수]
- `shouldMask`: 개인정보 마스킹 여부 [옵션, 기본값: true]

**응답**:
```json
{
  "id": "member-uuid",
  "name": "홍*동",                    // 마스킹 처리
  "phoneNumber": "+8210****5678",    // 마스킹 처리
  "memNo": "123****890",              // 마스킹 처리
  "grade": "AS03"                     // 멤버십 등급
}
```

## 에러 코드

| 코드 | HTTP 상태 | 설명 | 대응 방법 |
| --- | --- | --- | --- |
| FDE-C-001 | 400 | 요청 데이터가 유효하지 않음 | 요청 형식 및 필수 파라미터 확인 |
| FDE-A-001 | 401 | 인증 오류 | 유효한 JWT 토큰으로 요청 |
| FDE-A-002 | 403 | 권한 부족 또는 비즈니스 로직 제약 위반 | 오류 메시지의 세부 내용 확인 |
| FDE-C-002 | 404 | 요청한 리소스를 찾을 수 없음 | 올바른 ID 확인 |
| FDE-C-003 | 409 | 고유 제약 조건 실패(중복 데이터) | 이미 존재하는 데이터인지 확인 |
| FDE-C-004 | 409 | 외래 키 제약 조건 실패 | 연결된 데이터 먼저 처리 후 재시도 |
| FDE-C-101 | 500 | 내부 서버 오류 | 서버 관리자에게 문의 |

## 비즈니스 로직

### 예약 생성 플로우

1. 회원이 예약 컨텐츠와 시간대 선택
2. 약관 동의 확인 (필수 약관 체크)
3. 쿠폰 사용 여부 확인
4. `confirmConfig`에 따라 예약 상태 결정:
   - 즉시 확정: CONFIRMED 상태로 생성
   - 관리자 승인: PENDING 상태로 생성
   - 추첨: PENDING 상태로 생성 후 추첨 진행
5. 예약 생성 및 이력 기록
6. 쿠폰 사용 시 상태 업데이트

### 예약 상태 관리

- **PENDING → CONFIRMED**: 관리자 승인 또는 추첨 당첨
- **CONFIRMED → USED**: QR 스캔 또는 관리자 수동 처리
- **CONFIRMED → NOSHOW**: 사용일 다음날 자동 처리
- **모든 상태 → CANCELLED**: 취소 처리

### 권한별 가능 작업

- **사용자**: 예약 생성, 수정, 취소 (PENDING/CONFIRMED 상태에서만)
- **관리자**: 모든 상태에서 수정/취소 가능, 상태 직접 변경 가능
- **시스템**: 추첨, 만료, 노쇼 등 자동 처리

## 예제 코드

### tRPC 클라이언트 사용 예제

```typescript
import { createTRPCProxyClient, httpBatchLink } from '@trpc/client';

const client = createTRPCProxyClient<AppRouter>({
  links: [
    httpBatchLink({
      url: 'https://api.example.com/trpc',
      headers() {
        return {
          authorization: `Bearer ${getToken()}`,
        };
      },
    }),
  ],
});

// 예약 생성
const reservation = await client.reservation.create.mutate({
  timeSlotId: 'timeslot-uuid',
  groupSize: 2,
  phoneNumber: '+821012345678',
  termsAgreements: [
    { termsId: 'terms-uuid', isAgreed: true }
  ]
});

// 예약 목록 조회
const myReservations = await client.reservation.me.reservations.query({
  statusIn: ['CONFIRMED'],
  page: 1,
  pageSize: 10
});
```

### REST API 사용 예제

```javascript
// 예약 생성
const response = await fetch('https://api.example.com/external/app/reservation/create', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  body: JSON.stringify({
    timeSlotId: 'timeslot-uuid',
    groupSize: 2,
    phoneNumber: '+821012345678',
    termsAgreements: [
      { termsId: 'terms-uuid', isAgreed: true }
    ]
  })
});

const result = await response.json();
console.log('예약 ID:', result.id);
console.log('예약 번호:', result.sn);
```